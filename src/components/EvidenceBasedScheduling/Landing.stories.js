import React from 'react';
import Landing from './Landing';

export default {
  title: 'EvidenceBasedScheduling/Landing',
  component: Landing,
};

function Template() {
  return <Landing />;
}

export const Default = Template.bind({});
