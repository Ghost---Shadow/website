import React from 'react';

import BlogShell from '.';

export default {
  title: 'Components/BlogShell',
  component: BlogShell,
};

function Template() {
  return <BlogShell />;
}

export const Default = Template.bind({});
Default.args = {};
