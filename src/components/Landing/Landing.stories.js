import React from 'react';

import Landing from '.';

export default {
  title: 'Components/Landing',
  component: Landing,
};

function Template() {
  return <Landing />;
}

export const Default = Template.bind({});
Default.args = {};
